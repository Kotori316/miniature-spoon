services:
  node:
    image: node:20
    volumes:
      - ./workers:/root/workers
      - container_node_modules:/root/workers/node_modules
    init: true
    command: sleep inf

volumes:
  container_node_modules:
